---
id: concepts
title: Concepts
sidebar_label: Concepts
slug: /concepts
---

import { ConceptsChart } from '../../src/components/ConceptsChart';

## 什么是 RPC？应该拥有什么样的思维模式？

### 它只是函数

RPC is short for "Remote Procedure Call". It is a way of calling functions on one computer (the server) from another computer (the client). With traditional HTTP/REST APIs, you call a URL and get a response. With RPC, you call a function and get a response.
RPC 是 "Remote Procedure Call" 的缩写，它是一种在一台计算机（客户端）上调用另一台计算机（服务器）上面的函数的方式。在传统的 HTTP/REST API 中，你需要调用一个 URL 并获得一个响应。使用 RPC 后，则是调用一个函数并获得一个响应。

```ts
// HTTP/REST
const res = await fetch('/api/users/1');
const user = await res.json();

// RPC
const user = await api.users.getById({ id: 1 });
```

### 不要考虑 HTTP/REST 的实现细节

如果你检查 tRPC 应用程序的网络流量，你会发现它只是相当标准的 HTTP 请求和响应。但是在编写应用程序代码时，你不需要考虑实现细节。你调用函数，tRPC 会处理其他所有事情。

## 术语

以下是在 tRPC 生态系统中经常使用的一些术语。我们将在整个文档中使用这些术语，所以熟悉它们是很好的。这些概念中的大多数也在文档中有自己的页面。

<ConceptsChart />
